import { CodelineDirective } from './codeline.directive';

describe('CodelineDirective', () => {
  it('should create an instance', () => {
    const directive = new CodelineDirective();
    expect(directive).toBeTruthy();
  });
});
